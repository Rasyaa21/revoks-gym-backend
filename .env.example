APP_NAME=revoks-gym-backend
APP_PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=fiber_gorm_db
DB_SSLMODE=disable
DB_TIMEZONE=Asia/Jakarta

# Auth
JWT_SECRET=change-me
# Optional (default 24h)
JWT_ACCESS_TTL_MINUTES=1440

# Development
# Set true to insert demo data (templates, PT, schedules) when database is empty
DB_SEED=false
